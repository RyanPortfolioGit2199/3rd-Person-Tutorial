using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//When creating a Script in Unity it defaults to a monobehavior which is a script to be placed on a Gameobject. States dont need a Monobehavior so get rid of it but that also takes away the start and update functions.
public abstract class State   // An abstract class is a restricted class that cannot be used to create objects(to access it, it must be inherited from another class)
{

}
